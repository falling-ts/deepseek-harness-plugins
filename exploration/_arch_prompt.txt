Deep architectural analysis of the monorepo at D:\deepseek-harness-plugins\deepseek-harness. Produce a maximal-length, technically precise engineering report across three parts, citing specific files/dirs and quoting key type signatures/constants verbatim, with ASCII diagrams where helpful:
PART 1 Repository topology & build system — enumerate every package/app (apps/*, packages/*, examples/*), each member's role and its inter-member dependencies, how the pnpm workspace wires them, the build/publish pipeline, TS project references, and any codegen steps.
PART 2 Core abstractions — the RpcMethodMap and how the 74-method S1 unary RPC surface is compiled/locked; every MuxFrame variant and every HostFrame variant explained; how S4 gateway dispatch distinguishes dot-form (/api/ns.method) vs slash-form (/api/ns/method) routing; the settings document schema; the credential/model registry.
PART 3 Session & context management — session lifecycle; the token-metering/projection machinery (contextPressure, projectedTokens, rawSweep, surface vs total); how compaction checkpoints fire and how the flush gate ties to projectedTokens; relevant data structures.
Be exhaustive; begin with Part 1 and continue through Part 3 in one continuous report.
